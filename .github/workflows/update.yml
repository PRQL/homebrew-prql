name: update-brew
on: workflow_dispatch
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "the dispatch is working on a new release ${{ inputs.version }}, ${{ inputs.URL }}"